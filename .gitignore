# Rust build artifacts
/target/
Cargo.lock
rust_out

# Workspace temp files
*.code-workspace.lock
*.code-workspace.tmp

# Local assistant / generated metadata
/.claude/
.mermaid/
.rules
.rules.md
mcp-fast-ctx-usage.md

# Runtime logs (generated locally)
/logs/

# Zed editor workspace settings (generated locally)
/.zed/

# OS / editor noise
.DS_Store
*.swp
*.swo
*~

# Generic logs / temp
*.log
*.log.*
*.tmp

# Built dylib (rebuild via `cargo build --release` — symlink target only)
/libzed_yolo_hook.dylib

# Personal multi-folder workspace files (user-local, contain absolute paths)
/zed-yolo.code-workspace
